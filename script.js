var lol=prompt("Enter your shape")
if(lol==="trapezoid"){
    var tr1=prompt("Enter the top base.")
    var tr2=prompt("Enter the bottom base.")
    var tr3=prompt("Enter the height.")
    confirm((tr1+tr2)*(tr3)/2)
}